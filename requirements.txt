# Core data and computation
yfinance>=0.2.28
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Optimization
cvxpy>=1.3.0
scikit-learn>=1.3.0

# Risk models (GARCH, DCC)
arch>=6.2.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Dashboard
streamlit>=1.28.0

# Additional utilities
tqdm>=4.65.0

# AI explanations (optional)
openai>=1.12.0

# Report generation (optional - for PDF conversion)
markdown>=3.4.0  # For markdown to HTML conversion
weasyprint>=60.0  # For HTML to PDF conversion
# pypandoc>=1.12  # Optional: For better LaTeX support in PDFs (requires pandoc binary)

# Time series forecasting (optional - install as needed)
statsmodels>=0.14.0  # For ARIMA, exponential smoothing
prophet>=1.1.4  # For Prophet forecasting (requires: pip install prophet)
tensorflow>=2.13.0  # For LSTM, TCN, and Transformer models
xgboost>=2.0.0  # For XGBoost forecasting
