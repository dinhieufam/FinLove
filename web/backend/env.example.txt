# CREATE a .env file in the following content

# Multiple API keys (comma-separated, for rotation)
GEMINI_API_KEYS=key1,key2,key3

# Multiple models (comma-separated, for rotation)
GEMINI_MODELS=gemini-2.5-pro,gemini-2.5-flash,gemini-2.5-flash-lite,gemini-2.0-flash,gemini-2.0-flash-lite

# Embedding model
HUGGINGFACE_EMBEDDING_MODEL=all-MiniLM-L6-v2
